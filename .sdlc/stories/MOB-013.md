**Durum**: Done
**Öncelik**: Yüksek (Faz 6 — A5 bulgusu)
**Tahmin**: S

## Kaynak
2026-09-17 UI/UX denetimi, bulgu A5: dağınık küçük sabit renk kalıntıları — `dashboard_screen.dart`, `receipt_detail_screen.dart`, `ocr_screen.dart`, `categories_screen.dart`, `household_screen.dart`.

## Kabul Kriterleri
- Her dosyadaki A5 bulgusu tek tek değerlendirilip (gerçekten `AppColors`'da bir karşılığı varsa) düzeltilmeli.
- `flutter analyze`/`flutter test` regresyon olmadan geçmeli.

## Çıktı / Doğrulama
- **`dashboard_screen.dart:283,297,340`**: 3 hızlı-eylem kartının rengi ham hex yerine `AppColors` token'larına bağlandı: `0xFFFF6B6B` → `AppColors.accent` (tam eşleşme yoktu, en yakın anlamlı vurgu rengi), `0xFF8B5CF6` → `AppColors.categoryUlasim` (TAM eşleşme, aynı hex değeri), `0xFF10B981` → `AppColors.categoryMarket` (TAM eşleşme, aynı hex değeri).
- **`receipt_detail_screen.dart:230-233`**: incelendi, GERÇEKTE bir sorun yoktu — gradient zaten `AppColors.primary`/`primaryDark` kullanıyor (muhtemelen daha önceki bir refactor'da düzelmiş). Değişiklik yapılmadı.
- **`ocr_screen.dart:300-301`**: elle yazılmış `Theme.of(context).brightness == Brightness.dark ? 1 : 0.6` alfa hesaplaması kaldırıldı — `AppColors.primDim(context)` zaten hem açık hem koyu tema için doğru dim tonu döndürüyor, ek hesaplama gereksiz karmaşıklıktı ve tema sisteminin amacını atlıyordu.
- **`categories_screen.dart:14,16`**: kategori renk seçeneklerindeki `Color(0xFFE91E63)`/`Color(0xFFFF5722)` → `AppColors.categoryGiyim`/`AppColors.categoryRestoran` (mevcut, isimlendirilmiş kategori renk token'ları).
- **`household_screen.dart:337`**: "Yeni Aile Oluştur" kartındaki eski mor gradient (`0xFF6C63FF`/`0xFF9C8FFF`, MOB-012'de statistics/spending_personality ekranlarında da bulunan aynı eski palet) → `AppColors.primary`/`primaryLight`.
- **Doğrulama**: her dosya `flutter analyze` ile ayrı ayrı "No issues found!" verdi. `flutter analyze lib test` → 3 (MOB-006'dan kalan kapsam dışı, değişmedi). `flutter test` → 20/20 geçti, regresyon yok.

## Öğrenilen Dersler
- Bir denetim bulgusunu kör kör uygulamak yerine önce doğrulamak değerli: `receipt_detail_screen.dart`'taki A5 maddesi incelendiğinde ARTIK GEÇERSİZ olduğu (muhtemelen ara bir commit'te düzelmiş) ortaya çıktı — "bulgu var, düzelt" yerine "bulgu hâlâ var mı, öyleyse düzelt" yaklaşımı gereksiz/yanlış bir değişiklik yapmayı önledi.
- Aynı eski mor palet (`0xFF6C63FF`/`0xFF9C8FFF`) hem MOB-012'de (statistics/spending_personality) hem burada (household_screen) tekrar çıktı — bu, tek bir "eski tema"nın kod tabanına birden fazla noktadan sızdığını gösteriyor; ilerideki bir grep taraması (`grep -rn "0xFF6C63FF\|0xFF9C8FFF"`) benzer kalıntıları erken yakalayabilir.
