**Durum**: Done
**Öncelik**: Yüksek (Grup 2 işlevsel denetim, madde 1)
**Tahmin**: S

## Kaynak
Grup 2 (kalan 19 ekran) denetiminde bulunan bulgu: `spending_calendar_screen.dart` ve `spending_personality_screen.dart` MOB-011'in 9 dosyalık `ErrorStateWidget`/`LoadingStateWidget` rollout'unda ATLANMIŞTI — hâlâ eski `_errorMessage` deseniyle, "Tekrar Dene" butonu OLMADAN düz bir hata metni gösteriyorlardı. Kullanıcı, veri çekilemediğinde çıkmaz sokakta kalıyordu (tek gizli çıkış: keşfedilemez pull-to-refresh).

## Kabul Kriterleri
- İki ekran da MOB-010/011'deki standart `LoadingStateWidget`/`ErrorStateWidget` desenine geçmeli.
- "Tekrar Dene" butonu çalışmalı (ilgili `_load*` metodunu tekrar çağırmalı).
- `flutter analyze`/`flutter test` regresyon olmadan geçmeli, gerçek emulator'da kanıtlanmalı.

## Çıktı / Doğrulama
- **`spending_calendar_screen.dart`**: `String? _errorMessage` → `Object? _loadError` (MOB-011'deki AYNI dönüşüm — artık ham exception saklanıyor, çeviri `ErrorStateWidget`'ın içinde yapılıyor). `Center(child: CircularProgressIndicator(...))` → `LoadingStateWidget()`. Düz `Center(child: Text(_errorMessage!))` → `ErrorStateWidget(error: _loadError!, onRetry: _loadReceipts, fallback: 'Fişler alınamadı, lütfen tekrar deneyin.')`. Artık gereksiz olan `network_error.dart` import'u kaldırıldı.
- **`spending_personality_screen.dart`**: birebir aynı dönüşüm, `onRetry: _load`.
- **Görsel doğrulama (gerçek emulator, `emulator-5554`)**: geçici bir debug harness (`lib/main_debug_screens.dart`, iş bitince SİLİNDİ) ile her iki ekran da hata durumunda artık standart bulut-ikonu + "Tekrar Dene" butonu gösterdiği ekran görüntüsüyle kanıtlandı (backend oturumu olmadığı için doğal olarak hata durumuna düşüyorlar — bu senaryo GERÇEK bir "veri çekilemedi" durumunu birebir simüle ediyor).
- Test cihazı temizliği: üretim giriş noktası yeniden derlenip emulator'e geri yüklendi, debug harness dosyası silindi.
- **Doğrulama**: `flutter analyze lib/screens/spending_calendar_screen.dart lib/screens/spending_personality_screen.dart` → "No issues found!". `flutter analyze lib test` → 5 (MOB-006/add_receipt_screen'den kalan kapsam dışı infolar, değişmedi). `flutter test` → 61/61 geçti, regresyon yok.

## Öğrenilen Dersler
- MOB-011'deki "9 dosyalık rollout" listesi orijinal UI/UX denetim raporundaki "ham hata mesajları" bulgu listesinden türetilmişti — ama o liste `grep` ile "$e" deseni arayarak çıkarılmıştı, `_errorMessage`/düz-metin-hata-ama-retry-yok deseni farklı bir arama gerektiriyordu (`ErrorStateWidget` YOKLUĞU). Bu, "bir denetimin bulduğu liste tam listedir" varsayımının riskli olabileceğini gösteriyor — aynı KÖK SORUNUN (kötü hata deneyimi) farklı BELİRTİLERİ (ham exception metni VS retry butonu eksikliği) farklı grep desenleri gerektirebiliyor, ikinci bir geçiş (bu Grup 2 denetimi gibi) gözden kaçanları yakalayabiliyor.
