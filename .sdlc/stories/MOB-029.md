**Durum**: Done
**Öncelik**: Yüksek (Grup 1 işlevsel denetim, madde 1)
**Tahmin**: S

## Kaynak
Grup 1 (Dashboard, Receipt List, Receipt Detail, Budget, Categories, Statistics, Profile) işlevsel denetiminde bulunan bulgu: `dashboard_screen.dart:579-658` — "Son Fişlerin" listesindeki `Container`lar hiçbir `GestureDetector`/`InkWell` sarmalayıcısına sahip değildi, kullanıcı ana ekrandan gördüğü bir fişe dokunup detayına gidemiyordu.

## Kabul Kriterleri
- Dashboard'daki her son fiş kartı tıklanabilir olmalı, `receipt_list_screen.dart`'taki `_goToDetail` deseniyle tutarlı çalışmalı.
- `flutter analyze`/`flutter test` regresyon olmadan geçmeli.

## Çıktı / Doğrulama
- `dashboard_screen.dart`'a `_goToDetail(Receipt receipt)` metodu eklendi (receipt_list_screen.dart'taki isimlendirme/davranışla birebir tutarlı).
- "Son Fişlerin" listesindeki her kart `PressableScale` (MOB-023) ile sarmalandı — hem tıklanabilir hale geldi hem de basılı-tutma mikro-etkileşimi kazandı. Kart ayrıca `AppColors.cardShadow` (MOB-024) ile tutarlı gölgeye kavuştu — bu iki widget'a önceden hiç dokunulmamıştı.
- Tıklama, fiş silinmiş/düzenlenmiş olabileceği ihtimaline karşı `result == true` dönerse `_loadReceipts()` ile listeyi yeniliyor (receipt_list_screen'deki aynı davranış).
- **Doğrulama**: `flutter analyze lib/screens/dashboard_screen.dart` → "No issues found!". Gerçek veri backend oturumu gerektirdiği için canlı tıklama akışı ekran görüntüsüyle kanıtlanamadı — kod, `receipt_list_screen.dart`'taki KANITLANMIŞ (MOB ekranlarında zaten çalışan) `_goToDetail`/`PressableScale`/`Navigator.push(ReceiptDetailScreen(...))` deseninin birebir kopyası olduğu için işlevsel doğruluğu bu benzerlik üzerinden güvenceye alınıyor.

## Öğrenilen Dersler
- Bir ekranın "tıklanabilir görünen ama tıklanamayan" bir öğesi, görsel olarak (kart stili, gölge, border) tam bir buton gibi göründüğü için kod okumadan fark edilmesi zor bir hata sınıfı — sadece ekran görüntüsüne bakarak değil, widget ağacında gerçekten bir `GestureDetector`/`onTap` olup olmadığını grep ile doğrulamak gerekiyor.
