**Durum**: Done
**Öncelik**: Orta (Faz 8/3 — "cila turu", O3 bulgusu)
**Tahmin**: S

## Kaynak
2026-09-17 UI/UX denetimi, bulgu O3: `dashboard_screen.dart:245-350` — 6-7 eylem kartı ("Fiş Ekle", "Tüm Fişler", "İstatistikler", "Fişi Tara", "Bütçe", "Rapor Al", "Ekstre İçe Aktar") hepsi aynı boyut/ağırlıkta alt alta sıralanmış. En kritik aksiyon ("Fiş Ekle") öne çıkmıyor, FAB da yok.

## Kabul Kriterleri
- "Fiş Ekle" diğer 6 aksiyondan görsel olarak ayrışmalı.
- Mevcut tasarım dili (AppColors, kart stilleri, diğer ekranlardaki FAB deseni) referans alınmalı, yeni bir stil icat edilmemeli.
- `flutter analyze`/`flutter test` regresyon olmadan geçmeli, gerçek cihazda görsel olarak kanıtlanmalı.

## Çıktı / Doğrulama
- **`dashboard_screen.dart`**: "Fiş Ekle" üst satırdaki `Row`'dan çıkarılıp `Scaffold.floatingActionButton`'a taşındı — `FloatingActionButton.extended(icon: Icons.add, label: 'Fiş Ekle')`, budget/categories/receipt_list ekranlarında zaten kullanılan standart FAB deseniyle birebir aynı (yeni bir stil icat edilmedi). "Tüm Fişler" artık tek başına tam genişlikte bir `_QuickActionCard`, diğer 5 kart (İstatistikler/Fişi Tara/Bütçe/Rapor Al/Ekstre İçe Aktar) değişmeden altında sıralı duruyor.
- Navigator push + `_loadReceipts()` mantığı (önceden "Fiş Ekle" kartının `onTap`'inde) değişmeden `_addReceipt()` adlı ayrı bir metoda taşındı, FAB'ın `onPressed`'i buna bağlandı.
- **Görsel doğrulama (gerçek emulator, `emulator-5554`)**: geçici bir debug harness (`lib/main_debug_screens.dart`, `DashboardScreen`'i doğrudan auth olmadan render eden — iş bitince SİLİNDİ) ile hem açık hem koyu temada ekran görüntüsü alındı. "Fiş Ekle" artık belirgin, indigo, genişletilmiş bir FAB olarak sağ altta duruyor; "Tüm Fişler" tek başına üstte, diğer 5 kart altında aynı ağırlıkta — hiyerarşi net.
- Test cihazı temizliği: gerçek üretim giriş noktası (`lib/main.dart`) yeniden derlenip emulator'e geri yüklendi, debug harness dosyası silindi.
- **Doğrulama**: `flutter analyze lib/screens/dashboard_screen.dart` → "No issues found!". `flutter analyze lib test` → 3 (MOB-006'dan kalan kapsam dışı, değişmedi). `flutter test` → 30/30 geçti, regresyon yok.

## Öğrenilen Dersler
- Gerçek bir backend oturumu/veri gerektiren bir ekranın (dashboard, network'e bağımlı) görsel hiyerarşi değişikliğini kanıtlamak için MOB-012'de kurulan "geçici debug harness ile auth'u atla" deseni burada da işe yaradı — dashboard'un boş/hata durumunda bile (fiş verisi olmadan) düzen/hiyerarşi kanıtlanabildi, çünkü değişiklik veri-bağımlı değil saf layout meselesiydi.
- Tek çocuklu bir `Row(children: [Expanded(child: ...)])` kalıbını basitleştirip doğrudan widget'ı kullanmak (gereksiz `Row`/`Expanded` sarmalayıcısını kaldırmak), "Fiş Ekle" kartını çıkarınca ortaya çıkan bir fırsat oldu — küçük ama okunabilirliği artıran bir temizlik.
