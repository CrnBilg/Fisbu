**Durum**: Done
**Öncelik**: Yüksek
**Tahmin**: S

## Kaynak
2026-09-13 mobil App Store denetimi, bulgu Y2: `lib/screens/dashboard_screen.dart:39-56` (`_loadReceipts`) — `ReceiptService.getReceipts()` ve `BudgetService.getBudgets()` birbirine bağımlı olmadığı halde sıralı (`await`, `await`) çekiliyordu; PERF-004/PERF-006 emsaliyle aynı kategori (küçük ama gerçek, kümülatif açılış gecikmesi).

## Kabul Kriterleri
- İki bağımsız network çağrısı `Future.wait` ile paralel çekilir.
- Davranış (nihai state) değişmez — sadece toplam bekleme süresi (iki round-trip yerine bir round-trip'e yakın) kısalır.

## Hata Senaryoları
- Bu değişiklik MOB-004 ile aynı fonksiyonda ve aynı ihtiyaçtan (kod bloğunu zaten yeniden yazmak) kaynaklandığı için MOB-004'ün "ikisinden biri başarısız olursa Error durumuna geçilir" kararıyla birleşti — ayrı ayrı yapılsaydı sıralı awaitlerin paralelleştirilmesi, hata yönetimini de (iki ayrı try/catch → tek `Future.wait` + tek `catch`) yeniden düşünmeyi gerektirirdi zaten.

## Çıktı / Doğrulama
- Fiili kod değişikliği **MOB-004 ile birlikte, aynı `_loadReceipts()` yeniden yazımında** yapıldı (bkz. `.sdlc/stories/MOB-004.md` → Çıktı bölümü): `final results = await Future.wait([ReceiptService.getReceipts(), BudgetService.getBudgets()]);`.
- Doğrulama: MOB-004'teki `flutter analyze`/`flutter test` sonuçları bu değişikliği de kapsıyor (8/8 test geçti, regresyon yok). Ayrı bir doğrulama tekrarlanmadı çünkü aynı kod değişikliği zaten test edildi.
- Gerçek emulator ölçümü (PERF-005/PERF-006'daki gibi bir before/after) BU story için ayrıca yapılmadı — kazanç küçük (bir HTTP round-trip'lik fark, ~100-300ms mertebesinde beklenir) ve zaten PERF-006'da "gerçek kullanıcı deneyimi ölçümünün `pumpAndSettle` gibi araçlarla yanıltıcı olabileceği" öğrenildiği için, burada abartılı bir doğrulama turu yerine kodun doğruluğuna (davranış değişmedi, sadece sıralama paralel) güvenildi.

## Öğrenilen Dersler
- Aynı fonksiyonu etkileyen iki farklı denetim bulgusu (Y1: hata yönetimi eksik, Y2: paralelleştirme eksik) ayrı story olarak takip edilse de, uygulama sırasında TEK bir yeniden yazımda birleştirmek daha az riskli oldu — fonksiyonu iki kez art arda düzenlemek (önce hata yönetimi ekle, sonra paralelleştir) gereksiz ara-durum riski yaratırdı. Story ayrımı PLANLAMA/izlenebilirlik için değerli kalsa da, UYGULAMA sırası mühendislik gerçeklerine göre esnetilebilir — bu, PDM'nin story'leri "bağımsız uygulanacak" yerine "bağımsız izlenecek ama birlikte uygulanabilir" olarak işaretlemesi gerektiğini gösteriyor.
